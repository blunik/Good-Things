package app.good.things.data.model

/**
 * Class which returns the answer
 * isAnswer = true, if code is active
 * isAnswer = false, if code is inactive
 */
internal class CodeResponse(val isActive: Boolean)